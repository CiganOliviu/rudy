<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>
    <?php
    /*
      require_once ("../databases/mysql/mysqlAdministration.php");
      use databases\ServerAttributes;
      use databases\DatabasesApplications;

      $attributes = new ServerAttributes;
      $dbWorkFlow = new DatabasesApplications;

      if ($dbWorkFlow->StabiliseConnection ($attributes))
        echo("<p>Connection stabilised</p>");

      if ($dbWorkFlow->CloseConnection($dbWorkFlow->StabiliseConnection ($attributes)))
        echo("<p>Connection closed</p>");

      require_once ("../interogations/interogationsAdministration.php");
      require_once ("../databases/mysql/setupSystemDatabases.php");
    */
    ?>
    <h1> Welcome </h1>
  </body>
</html>
